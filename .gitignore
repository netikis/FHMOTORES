# Segredos e ambiente local (NUNCA subir ao GitHub)
.env
.env.local
.env.*.local
.env.development.local
.env.production.local

# Gerado no build (npm run build / GitHub Actions) — não versionar
firebase-env.js

# Dependências e deploy
node_modules/
.vercel/
dist/
*.log
npm-debug.log*

# Credenciais / backups locais
FIREBASE/firebase-chave.js
FIREBASE CHAVE KEY.txt
chave api/
*.backup.html
backup/
_audio_work/

# SO / editor
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
